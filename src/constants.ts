import { Activity, Expense, Memory } from './types';

// These constants are no longer used as the app now fetches data from Supabase.
// They are kept as empty arrays to maintain the exports if needed elsewhere, 
// or you can remove them entirely if you are sure they aren't imported.

export const ACTIVITIES: Activity[] = [];
export const EXPENSES: Expense[] = [];
export const MEMORIES: Memory[] = [];
